version: '3.4'

services:
  nginx:
    image: nginx:latest
    container_name: nginx
    restart: unless-stopped
    ports:
      - 80:80
      - 443:443
    volumes:
      - ./nginx/prod.conf:/etc/nginx/nginx.conf
      - ./nginx/live:/root/.ssl/
